# CSCI-271-Fall-2026
My name is Steven Jimenez and this is my practice repo for this course.

# Assigment 1
The program here is running a code where it prints "Hello World!" as a first line inside the terminal followed by another line of code that prints a quesiton and then ask for the users input using cin as a way to input your answer onto the terminal then using that data inputted to output once again to the terminal but adding "Nice to meet you," followed by x and the value stored inside the string x from perviously from the user.


# Assigment 2
The program is simillar to last's weeks Assigment but we are adding a extra line of code to it which is "int y". int y is a variable that is being declared as a integer so when we ask the user to type their age, it saves the number as a value inside y where we then can use it to print out the users name and age in the same sentence without error. both int y and string x are saving 2 different values, where x is saving the name and y is saving the age. then we use cout to print out the message introduce the user on the terminal.